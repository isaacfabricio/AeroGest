import { describe, it, expect } from 'vitest';

describe('Aplicação de Gerenciamento de Voos', () => {
    it('deve retornar verdadeiro para a soma de 1 + 1', () => {
        expect(1 + 1).toBe(2);
    });
});

// sistema-gerenciamento-voos/app.test.js
import { describe, it, expect } from 'vitest';

describe('Teste inicial', () => {
  it('deve passar no teste de exemplo', () => {
    expect(1 + 1).toBe(2);
  });
});